<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>signup page</title>
    <link rel="stylesheet" href="/css/common.css">

<style>
body {
	background-color: #F9FAFB;
	margin: 0;
}


.header {
	position: fixed;
	top:0;
	left:0;
	width:100%;
	padding: 0 80px;
	height: var(--header-height);
	background: transparent;
	z-index: 100;
	border-bottom: 0.3px solid #43554A;
	backdrop-filter: blur(3px);
}

.header-container {
	height: 100%;
	display: flex;
	align-items: center;
	justify-content: space-between;
	padding: 0 120px;
}

.logo {
	font-size: 24px;
	font-weight: 500;
	letter-spacing: 0.1em;
}

.logo a {
	text-decoration: none;
	color: #43554A;
}

.nav {
	height: 100%;
}

.nav-list {
	display: flex;
	align-items: center;
	height: 100%;
	list-style: none;
	margin: 0;
	padding: 0;
}

.nav-item {
	height: 100%;
	display: flex;
	align-items: center;
}

.nav-link {
	font-size: 12px;
	font-weight: 400;
	line-height: 0.85;
	color: #43554A;
	text-decoration: none;
	padding: 30.5px 25px;
	display: flex;
	align-items: center;
	line-height: 1.3;
}

/* 회원가입 칸 */
.signup-wrapper {
	display: flex;
	justify-content: center;
	align-items: center;
	min-height: 100vh;
	padding: calc(var(--header-height) + 60px) 20px 60px;
}

.signup-container {
	width: 100%;
	max-width: 520px;
	background: white;
	border-radius: 8px;
	padding: 40px 35px;
	box-shadow: 0 2px 8px rgba(0,0,0,0.08);
}

.signup-title {
	font-size: 24px;
	font-weight: 600;
	color: #43564A;
	margin-bottom: 30px;
	text-align: center;
}

.signup-form {
    display: flex;
    flex-direction: column;
    gap: 22px;
}

.form-group {
	display: flex;
    flex-direction: column;
	margin-bottom: 20px;
}

.form-group label .usertype-label {
	font-size: 15px;
    font-weight: 500;
    line-height: 1.59em;
    color: #364153;
    margin-bottom: 8px;
}

.required {
    color: #F1512E;
    margin-left: 2px;
}

.form-group input[type="text"],
.form-group input[type="password"],
.form-group input[type="date"] {
	width: 100%;
	padding: 12px 15px;
	border: 1px solid #DFE4EA;
	border-radius: 8px;
	font-size: 15px;
	font-weight: 400;
	transition: border-color 0.3s;
	box-sizing: border-box;
	line-height: 1.33em;
}

.form-group input::placeholder {
    color: #99A1AF;
}

/* 라디오 버튼 */
.radio-group {
	display: flex;
	gap: 20px;
	margin-bottom: 20px;
	padding: 5px 0;
}

.radio-group input[type="radio"] {
	width: 18px;
	height: 18px;
	margin-right: 6px;
	cursor: pointer;
	accent-color: #43564A;
}

.radio-group label {
	display: flex;
	align-items: center;
	font-size: 14px;
	line-height: 1.59em;
	color: #333;
	cursor: pointer;
}

/* 회원가입 버튼 */
.btn-signup-submit {
	width: 100%;
	padding: 13px;
	background-color: #43564A;
	color: white;
	border: none;
	border-radius: 8px;
	font-size: 15px;
	font-weight: 500;
	cursor: pointer;
	margin-top: 10px;
	text-align: center;
	line-height: 1.59em;
	transition: background-color 0.3s;
}

.btn-signup-submit:hover {
	background-color: #3a4a3a;
}

</style>
</head>

<body>
<header class="header">
    <div class="header-container">

        <div class="logo">
            <a href="/">Re:source</a>
        </div>

        <nav class="nav">
            <ul class="nav-list">
                <li class="nav-item">
                    <a href="/about" class="nav-link">ABOUT</a>
                </li>
                <li class="nav-item">
                    <a href="/analyze" class="nav-link">ANALYZE</a>
                </li>
                <li class="nav-item">
                    <a href="/mypage" class="nav-link">MY</a>
                </li>
                <li class="nav-item">
                    <a href="/login" class="nav-link nav-link-login">LOG OUT</a>
                </li>
            </ul>
        </nav>
    </div>
</header>


<div class="signup-wrapper">
    <div class="signup-container">
		<h2 class="signup-title">회원가입</h2>

	<form method="post" action="/signup">
		<input type="hidden" name="_csrf" value="{{_csrf.token}}">

		<div class="form-group">
			<label for="username">아이디 <span class="required">*</span></label>
			<input type="text" id="username" name="username"  placeholder="아이디를 입력해 주세요" value="{{signupRequest.username}}">
		</div>

		<div class="form-group">
			<label for="password">비밀번호 <span class="required">*</span></label>
			<input type="password" id="password" name="password" placeholder="비밀번호를 입력해 주세요" value="{{signupRequest.password}}">
		</div>

		<div class="form-group">
			<label for="name">이름 <span class="required">*</span></label>
			<input type="text" id="name" name="name" placeholder="이름을 입력해 주세요" value="{{signupRequest.name}}">
		</div>

		<div class="form-group">
			<label for="birthday">생년월일 <span class="required">*</span></label>
			<input type="date" id="birthday" name="birthday" value="birthday">
		</div>

		<div>
			<label class="usertype-label">소속</label>
			<div class="radio-group">
				<label>
					<input type="radio" id="individual" name="userType" value="individual" checked>
					개인
				</label>

				<label>
					<input type="radio" id="business" name="userType" value="business">
					사업자
				</label>
		</div>
		</div>

		<button type="submit" class="btn-signup-submit">회원가입 완료</button>
	</form>
	</div>
</div>

{{#message}}
<script>
	alert("{{message}}");
</script>
{{/message}}

</body>
</html>