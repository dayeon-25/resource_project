package com.example.resource.repository;

import com.example.resource.entity.RaRegion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RaRegionRepository extends JpaRepository<RaRegion, Long> {
}
